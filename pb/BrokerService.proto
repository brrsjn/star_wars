syntax = "proto3";

option go_package = "/pb";


message instruct {
    string message = 1;
    bool lectura =2;
}

message Servidor {
    string addres =1;
    bool Error=2;
} 


service Broker{
    
    rpc ConnectToServer(instruct) returns (Servidor){};     
}